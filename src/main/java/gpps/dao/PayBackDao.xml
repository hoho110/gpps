<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gpps.dao.IPayBackDao">
	<insert id="create" parameterType="gpps.model.PayBack"
		useGeneratedKeys="true" keyProperty="id">
		insert into
		PayBack(borrowerAccountId,productId,state,chiefAmount,interest,deadline)
		values(#{borrowerAccountId},#{productId},#{state},#{chiefAmount},#{interest},#{deadline})
	</insert>
	<select id="findAllByProduct" resultType="gpps.model.PayBack">
		select *
		from PayBack
		where productId=#{productId}
	</select>
	<select id="find" parameterType="Integer" resultType="gpps.model.PayBack">
		select *
		from ProductSeries where id=#{id}
	</select>
	<update id="changeState">
		update
		ProductSeries set state=#{state} where id=#{id}
	</update>
	<delete id="delete">
		delete from ProductSeries
		where
		id=#{id}
	</delete>
</mapper>